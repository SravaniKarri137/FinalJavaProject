package com.example.repository;

import org.springframework.context.annotation.Primary;
import org.springframework.stereotype.Repository;

@Repository
@Primary
public interface WishlistRepository extends CartRepository {
    // Inherits everything from CartRepository with isWishlist = true filtering in service
}